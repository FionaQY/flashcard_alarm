<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- RecyclerView for the list of lessons -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/lesson_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingHorizontal="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="80dp"
        android:scrollbars="vertical" />

    <!-- FAB: Add new lesson (bottom end) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addLessonFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:contentDescription="Add new Flashcard Set"
        android:src="@drawable/baseline_add_alarm_24"
        app:backgroundTint="@color/colorPrimary"
        app:tint="@android:color/white"
        android:elevation="6dp"
        android:onClick="onAddLessonClicked" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
